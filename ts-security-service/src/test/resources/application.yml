spring:
  application:
    name: ts-security-service
  data:
    mongodb:
      # uri: mongodb://localhost/ts
      host: localhost
      #      username: user
      #      password: ${MONGODB_PASSWORD}
      database: ts
      port: 27017

server:
  port: 11188