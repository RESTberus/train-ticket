server:
  port: 15678  # HTTP (Tomcat) port

spring:
  sleuth:
    sampler:
      probability: 1.0
    baggage:
      correlation-fields: x-request-id
    propagation:
      type: b3

  application:
    name: ts-verification-code-service


#opentracing:
#  jaeger:
#    udp-sender:
#      host: jaeger
#      port: 6831